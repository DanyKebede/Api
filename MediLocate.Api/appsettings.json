{

    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    // write a connection string to the postgres database
    "ConnectionStrings": {
        "DefaultConnection": "Host=localhost;Port=5432;Database=medilocate;Username=postgres;Password=password"
    }
}
